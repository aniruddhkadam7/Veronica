{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and every overlay window (Interview Mode, Meeting Mode). Tauri 2 scopes IPC/permissions per window label, so any window missing from this list silently loses every command invoke and window action (drag, etc.) even though the window itself still renders.",
  "windows": ["main", "interview-overlay", "meeting-overlay"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-is-maximized",
    "dialog:default",
    "fs:allow-read-file",
    "global-shortcut:default"
  ]
}
